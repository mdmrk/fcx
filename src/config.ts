export const devMode = __DEV__

export const oldSelectors = {
  feedContainer: "#posts",
}

export const newSelectors = {
  feedContainer: "#posts",
  resultsInfo: "span[title*='Mostrando resultados']",
}
